package effectivejava.enums_and_annotations.emulate_extensible_enums_with_interfaces;

// Emulated extensible enum using an interface
public interface Operation {
    double apply(double x, double y);
}
